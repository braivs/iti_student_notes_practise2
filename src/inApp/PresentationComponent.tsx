import React from "react";

export class PresentationComponent extends React.Component<PropsType> {

    // here we will put out logic from filter buttons

    render() {
        return (
            // here we will put out logic for filters (will set colander)
            <div>
                {/*
                    // drawing go:
                    // name of todolist
                    // <Component of moved INPUT />
                    // map
                    // filter buttons
                */}
            </div>
        )
    }

}

type PropsType = {}